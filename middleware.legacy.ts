/**
 * @deprecated Next.js 16+ uses `proxy.ts` instead of `middleware.ts`.
 * This file is intentionally left empty to avoid dual detection errors.
 * All route protection logic now lives in `proxy.ts`.
 */
export {};
